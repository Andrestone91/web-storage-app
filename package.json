{
  "name": "web-storage-desafio",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.6.2"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /web-storage-app",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^4.0.0"
  }
}
